import React from 'react';
import {Container, Heading} from '@chakra-ui/react';

const App: React.FC = () => {
    return (
        <Container>
            <Heading>Users list</Heading>
        </Container>
    );
};

export default App;
