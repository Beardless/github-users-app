import React from 'react';
import { Box, Container, Heading } from '@chakra-ui/react';

const App: React.FC = () => {
    return (
        <Container>
            <Heading>Users list</Heading>
        </Container>
    );
};

export default App;
